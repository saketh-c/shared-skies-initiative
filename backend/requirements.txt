fastapi
uvicorn[standard]
pandas
numpy
scikit-learn
lightgbm
xgboost
# Pin catboost to the version the v3b ensemble.joblib was trained with — keeps
# pickle compat stable across Render rebuilds. ~97 MB prebuilt cp311 wheel.
catboost==1.2.10
joblib
pyarrow
httpx
openpyxl
xlrd
dwave-neal
dimod
networkx
# NOAA HMS smoke polygon parsing — pure-Python (no GDAL dependency).
# shapely 2.x manylinux wheel bundles GEOS so no apt deps needed on Render.
pyshp==2.3.1
shapely==2.1.2
